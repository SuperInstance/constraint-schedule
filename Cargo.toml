[package]
rust-version = "1.70"
readme = "README.md"
name = "constraint-schedule"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/SuperInstance/constraint-schedule"
documentation = "https://docs.rs/constraint-schedule"
description = "Constraint-satisfaction scheduling for agent task allocation — CSP solver with AC-3 and simulated annealing"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
rand = "0.9"

[dev-dependencies]
